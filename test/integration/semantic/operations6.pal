{15,16,17}
{
    Test that operations work
}
program test(input, output);
var
	i : integer;
	b : boolean;
	r : real;
begin
	
	// /

	i := i / i;
	i := r / i;	// error
	i := i / r;	// error
	i := r / r;	// error
	
	r := i / i;
	r := r / i;
	r := i / r;
	r := r / r;
	
end.
