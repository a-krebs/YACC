{41,44,53}
{
	Cmput 415 - Team YACC
	7.pal
	Tests for correct syntatic use of operators.
	Errors:
		-missing close paren on line 41
		-missing open parent on line 44
		-comment separates characters in <> token making it
		 separate < and > tokens inappropriate place causing
		 error line 52

}

program PalPal(in, out);

const
	a = 19283940;
	b = 8*8*8*8*8*9*9*9*8*7*6*5*4*4;c = 230498230498;

type
	a = integer;
	b = real;
	c = bool;
	d = whatever;
	i = daTruth;
	hello = array[123..12312312312312312312] of adf;

var
	a : bone;
	b : cone;
	c : drone;
	d : eucardlkfjsldkfjlakdjfalkdsfjLKJLDSKJSDLKFJDLFKJ;
	i : amtired;

procedure p(a : integer);
begin
	a := (a < b)*(b-a*(a-b)*(2 div 5 div 3))*((((((((((0))))))))));
end;
begin
	a := (a*b-2) div (3/4)+(-2*((-2*(-2)))+111111111*bbbbbbbb;
	while (i*i*i*i*i*i > j*k*l/m/n/o/p) < -123123E897234987 do
		begin
			if a or b and c and d or c or d or b and not j) then
				nothing := nothingc
			else
				nothing := something;
			if a then
				continue
			else
				exit;
		end;
	if thatGravyTrainEveryStops <{comment separated tokens, should two tokens}> then
		weGot := problems
	else
		weGot := itMade;

end. 
